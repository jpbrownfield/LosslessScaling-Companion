# LS Companion dependencies
websockets>=12.0,<18
pydantic>=2.5.0,<3
psutil>=5.9.0
pywin32>=306
pystray>=0.19.5
Pillow>=10.0.0

# Test-only dependencies (not bundled into the packaged app)
pytest>=8.0.0
